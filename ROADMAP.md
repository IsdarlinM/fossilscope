# Roadmap

## Current — 0.3.x hardening
Implemented: Temporal graph, time travel, lifecycle states, historical API diff, separate historical/current confidence, resurrection detection, clustering/acquisition lineage, passive local adapters and bounded opt-in HTTPS collector runtime with cache/provenance.

Next:
- Expand provider-specific CT/DNS/archive/repository/package adapters over the implemented bounded HTTPS collector runtime, with richer quotas/terms metadata and provider compatibility tests.
- Temporal graph virtualization for large histories and stronger source-independence scoring.
- Historical identity/OAuth/SDK evolution diffing and stale-reference re-observation workflows.
- Cross-project correlation rules with Exposure DNA and TrustBoundary through shared SRIC workspaces.

## 1.0
Stable temporal schemas, source adapter SDK, reproducible archaeology datasets and independently validated fossil scoring.
